(
	state: Authenticated,
	greeting: (
		kind: PreAuth,
		code: Some(Capability(NonEmptyVec([Imap4Rev1, StartTls, Auth(Plain), Auth(Login)]))),
		text: Text("IMAP server ready.")
	),
	caps: [Imap4Rev1],
	caps_tls: [Imap4Rev1, StartTls, Auth(Plain), Auth(Login)],
	caps_auth: [Imap4Rev1],
	caps_tls_auth: [Imap4Rev1],

	pkcs12: (
		file: "certs/example.org.p12",
		password: "changeit",
	),
	implicit_tls: false,
)
