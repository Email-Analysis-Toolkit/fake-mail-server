(
	state: NotAuthenticated,

	// TODO: This doesn't work anymore in imap-codec > 0.8.0.
	// greeting: Ok(
	// 	tag: Some(Tag(inner: "XYZ")),
	// 	code: None,
	// 	text: Text("6Iy0xHRr")
	// ),

	greeting: (
		kind: Ok,
		code: None,
		text: Text("6Iy0xHRr")
	),

	caps:          [],
	caps_tls:      [],
	caps_auth:     [],
	caps_tls_auth: [],

	cert: Cert(
		crt_path: "<not needed>",
		key_path: "<not needed>",
	),

	implicit_tls: false,
)

